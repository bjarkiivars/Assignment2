const RetrieveTunes = () => {
    //Creating dependency variable.. I think
    const axios = require('axios');

    async function doGetRequest() {
        //The URL to which we will send the request
        const url = 'http://localhost:3000/api/v1/tunes';
    }
}